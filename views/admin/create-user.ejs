<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New User &lsaquo; Vertebra 2.0</title>
    <link href="/css/style.css" rel="stylesheet">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        }
    </style>
</head>

<body class="bg-[#f0f0f1] text-[#3c434a]">

    <div class="flex min-h-screen">
        <!-- Sidebar -->
        <%- include('../partials/admin-sidebar', { active: 'users' }) %>

            <!-- Main Content -->
            <div class="flex-grow flex flex-col">
                <!-- Top Bar -->
                <%- include('../partials/admin-header') %>

                    <div class="p-4 md:p-8">
                        <h1 class="text-2xl font-normal text-[#1d2327] mb-6">Add New User</h1>

                        <p class="mb-6 text-sm">Create a new user and add them to this site.</p>

                        <form action="/admin/users/create" method="POST" class="max-w-4xl space-y-6">
                            <div>
                                <label class="block text-sm font-semibold mb-1">Username <span
                                        class="text-red-500">(required)</span></label>
                                <input type="text" name="username" required
                                    class="w-full md:w-1/2 px-3 py-2 border border-[#8c8f94] focus:border-[#2271b1] focus:shadow-[0_0_0_1px_#2271b1] outline-none text-sm">
                            </div>

                            <div>
                                <label class="block text-sm font-semibold mb-1">Password <span
                                        class="text-red-500">(required)</span></label>
                                <input type="password" name="password" required
                                    class="w-full md:w-1/2 px-3 py-2 border border-[#8c8f94] focus:border-[#2271b1] focus:shadow-[0_0_0_1px_#2271b1] outline-none text-sm">
                            </div>

                            <div class="pt-4">
                                <button type="submit"
                                    class="bg-[#2271b1] hover:bg-[#135e96] text-white font-semibold py-2 px-6 rounded text-sm transition-colors border border-[#2271b1]">
                                    Add New User
                                </button>
                            </div>
                        </form>
                    </div>
            </div>
    </div>

</body>

</html>