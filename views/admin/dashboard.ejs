<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard &lsaquo; Vertebra 2.0</title>
    <link href="/css/style.css" rel="stylesheet">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen-Sans, Ubuntu, Cantarell, "Helvetica Neue", sans-serif;
        }
    </style>
</head>

<body class="bg-[#f0f0f1] text-[#3c434a]">

    <div class="flex min-h-screen">
        <!-- Sidebar -->
        <%- include('../partials/admin-sidebar', { active: 'dashboard' }) %>

            <!-- Main Content -->
            <div class="flex-grow flex flex-col">
                <!-- Top Bar -->
                <%- include('../partials/admin-header') %>

                    <div class="p-4 md:p-8">
                        <div class="flex justify-between items-center mb-6">
                            <h1 class="text-2xl font-normal text-[#1d2327]">Posts</h1>
                            <a href="/admin/create"
                                class="inline-block border border-[#2271b1] bg-[#f6f7f7] text-[#2271b1] hover:bg-[#f0f0f1] hover:border-[#135e96] text-sm font-semibold py-1 px-3 rounded transition-colors duration-200">
                                Add New
                            </a>
                        </div>

                        <!-- Stats & Notices -->
                        <div class="bg-white border border-[#c3c4c7] p-0 mb-6 shadow-sm">
                            <div class="border-b border-[#c3c4c7] px-4 py-3 bg-white font-semibold text-sm">At a Glance
                            </div>
                            <div class="p-4 text-sm">
                                <ul>
                                    <li class="mb-1"><a href="#" class="text-[#2271b1] font-semibold">
                                            <%= posts.length %> Posts
                                        </a></li>
                                    <li class="text-gray-500">WordPress 6.4.2 running Vertebra Theme.</li>
                                </ul>
                            </div>
                        </div>

                        <!-- Posts Table -->
                        <div class="bg-white border border-[#c3c4c7] shadow-sm">
                            <div class="flex items-center justify-between px-3 py-2 border-b border-[#c3c4c7] bg-white">
                                <div class="text-sm">
                                    <span class="mr-2">All (<%= posts.length %>)</span> | <span
                                        class="ml-2 text-[#2271b1]">Published (<%= posts.length %>)</span>
                                </div>
                            </div>
                            <table class="w-full text-left border-collapse text-sm">
                                <thead>
                                    <tr class="bg-white text-[#1d2327] border-b border-[#c3c4c7]">
                                        <th class="p-3 font-semibold w-10"><input type="checkbox"></th>
                                        <th class="p-3 font-semibold">Title</th>
                                        <th class="p-3 font-semibold">Author</th>
                                        <th class="p-3 font-semibold">Date</th>
                                    </tr>
                                </thead>
                                <tbody class="divide-y divide-[#c3c4c7]">
                                    <% if(posts.length> 0) { %>
                                        <% posts.forEach((post, index)=> { %>
                                            <tr
                                                class="<%= index % 2 === 0 ? 'bg-[#f6f7f7]' : 'bg-white' %> hover:bg-[#f0f0f1] group">
                                                <td class="p-3"><input type="checkbox"></td>
                                                <td class="p-3 relative">
                                                    <strong class="block text-[#2271b1] mb-1 font-semibold text-[14px]">
                                                        <a href="/admin/edit/<%= post._id %>">
                                                            <%= post.title %>
                                                        </a>
                                                    </strong>
                                                    <div
                                                        class="flex text-xs space-x-2 opacity-0 group-hover:opacity-100 transition-opacity duration-200">
                                                        <a href="/admin/edit/<%= post._id %>"
                                                            class="text-[#2271b1] hover:underline">Edit</a>
                                                        <span class="text-gray-300">|</span>
                                                        <form action="/admin/delete/<%= post._id %>?_method=DELETE"
                                                            method="POST" class="inline"
                                                            onsubmit="return confirm('Trash this post?');">
                                                            <button type="submit"
                                                                class="text-[#b32d2e] hover:underline bg-transparent border-0 p-0 cursor-pointer text-xs">Trash</button>
                                                        </form>
                                                        <span class="text-gray-300">|</span>
                                                        <a href="/post/<%= post.slug %>" target="_blank"
                                                            class="text-[#2271b1] hover:underline">View</a>
                                                    </div>
                                                </td>
                                                <td class="p-3"><a href="#" class="text-[#2271b1]">admin</a></td>
                                                <td class="p-3">
                                                    <span class="block text-gray-500">Published</span>
                                                    <%= new Date(post.createdAt).toLocaleDateString() %>
                                                </td>
                                            </tr>
                                            <% }) %>
                                                <% } else { %>
                                                    <tr>
                                                        <td colspan="4" class="p-6 text-center text-gray-400">No posts
                                                            found.</td>
                                                    </tr>
                                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
            </div>
    </div>

</body>

</html>